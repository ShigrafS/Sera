<UserControl
    x:Class="Sera.Controls.StreamingTextBlock"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sera.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="StreamingStates">
                <VisualState x:Name="Normal"/>
                <VisualState x:Name="Streaming">
                    <VisualState.Setters>
                        <Setter Target="TypingIndicator.Visibility" Value="Visible"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <TextBlock x:Name="ContentText"
                   Text="{x:Bind Text, Mode=OneWay}"
                   TextWrapping="Wrap"
                   MaxWidth="300"/>

        <StackPanel x:Name="TypingIndicator"
                    Orientation="Horizontal"
                    Visibility="Collapsed"
                    Spacing="4">
            <TextBlock Text="Sera is typing" Foreground="Gray" FontSize="12"/>
            <ProgressRing IsActive="True" Width="12" Height="12"/>
        </StackPanel>
    </Grid>
</UserControl>
