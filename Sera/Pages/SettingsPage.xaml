<Page
    x:Class="Sera.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sera.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="40">
        <StackPanel Spacing="32" MaxWidth="800" HorizontalAlignment="Left">
            
            <TextBlock Text="Settings" FontSize="36" FontWeight="Bold"/>

            <!-- Setup Notice (Visible only when API Key is missing) -->
            <InfoBar x:Name="SetupInfoBar"
                     Title="Setup Required"
                     Message="Please enter your NVIDIA API Key to enable AI task processing."
                     IsOpen="False"
                     Severity="Informational" />

            <!-- AI Configuration Section -->
            <StackPanel Spacing="12">
                <TextBlock Text="AI Configuration" FontSize="18" FontWeight="SemiBold"/>
                
                <Grid Padding="16" Background="{ThemeResource SystemControlBackgroundAltHighBrush}" CornerRadius="8">
                    <StackPanel Spacing="16">
                        <StackPanel Spacing="4">
                            <TextBlock Text="NVIDIA API Key" FontWeight="Medium"/>
                            <PasswordBox x:Name="ApiKeyBox" PasswordRevealMode="Hidden" PlaceholderText="nvapi-..." Width="400" HorizontalAlignment="Left"/>
                            <TextBlock Text="Your key is stored securely in the Windows Credential Locker." FontSize="12" Foreground="Gray"/>
                        </StackPanel>

                        <StackPanel Spacing="4">
                            <TextBlock Text="Model" FontWeight="Medium"/>
                            <ComboBox x:Name="ModelSelector" Width="400">
                                <ComboBoxItem Content="meta/llama-3.1-70b-instruct" IsSelected="True"/>
                                <ComboBoxItem Content="meta/llama-3.1-405b-instruct"/>
                                <ComboBoxItem Content="nvidia/nemotron-4-340b-instruct"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Spacing="8" Orientation="Horizontal">
                            <Button x:Name="SaveButton" Content="Save Settings" Click="SaveButton_Click" Style="{ThemeResource AccentButtonStyle}"/>
                            <Button x:Name="TestButton" Content="Test Connection" Click="TestButton_Click"/>
                        </StackPanel>

                        <TextBlock x:Name="TestResultText" FontSize="12" Visibility="Collapsed"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <!-- App Info Section -->
            <StackPanel Spacing="12">
                <TextBlock Text="About" FontSize="18" FontWeight="SemiBold"/>
                <Grid Padding="16" Background="{ThemeResource SystemControlBackgroundAltHighBrush}" CornerRadius="8">
                    <StackPanel Spacing="4">
                        <TextBlock Text="Sera AI Secretary" FontWeight="Medium"/>
                        <TextBlock Text="Version 1.0.0" FontSize="12" Foreground="Gray"/>
                        <TextBlock Text="Native Windows AI task management." FontSize="14" Margin="0,8,0,0"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
            
        </StackPanel>
    </ScrollViewer>
</Page>
